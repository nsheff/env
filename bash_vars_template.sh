# Set up bash environment variables.
# This is a template, to be adjusted individually on each system, and added to
# .bashrc
# These are all pointers to folders that should *include trailing slashes*

# Pointer to the 'raw data' filesystem
export RAW_DATA="/fhgfs/groups/lab_bsf/samples/"

# Pointer to the 'processed data' filesystem
export DATA_BASE="/fhgfs/groups/lab_bock/shared/projects/"

# Pointer to the common shared resources directory
export RESOURCE_DIR="/data/groups/lab_bock/shared/resources/"

# Pointer to the collection of git repos
export CODE_BASE="~/repo/"

# Legacy variable: just points to RGenomeUtils repo
export SHARE_DIR="~/rpack/RGenomeUtils/"

